# Node (the application, wherever harness.config.json points)
node_modules/
dist/
*.tsbuildinfo
.vite/

# Python (audit_log/ only)
__pycache__/
*.pyc
.pytest_cache/

.env

# Staged hook output, folded into the tracked logs by scripts/fold-logs.mjs
audit_log/.turns-pending.jsonl
audit_log/.subagents-pending.jsonl
audit_log/.usage-pending.md
