---
description: Run every gate and report pass or fail compactly
---

Run `bash scripts/gates.sh` and report the result.

If everything passes, say so in one line. If anything fails, show the failing
output and fix it — do not report a red gate back as a question when the fix is
in scope and you can see it.
